# Sphinx documentation

This directory contains a Read the Docs style Sphinx documentation source tree for the DRAN Pipeline package.

## Build

```bash
pip install -r requirements-docs.txt
make html
```

HTML output will be written to `build/html`.